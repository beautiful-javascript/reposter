function source(
  id, 
  fetchStrategyOptions, 
  parserOptions, 
  fetchStrategy, 
  parser) {
  return {
    id,
    fetchStrategyOptions,
    parserOptions,
    fetchStrategy,
    parser
  };
}

export default source;
